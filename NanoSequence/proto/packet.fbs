namespace nanodeploy.fbs;

enum Action : byte {
  StepOut = 0,
  AddRequest = 1,
  GetEngineInfo = 2,
  FreeSequences = 3,
  FreeVisionSlots = 4,
}

table ZmqPacket {
  action: Action;
  payload: [ubyte];
}

root_type ZmqPacket;
