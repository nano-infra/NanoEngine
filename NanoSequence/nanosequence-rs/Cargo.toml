[package]
name = "nanosequence-rs"
version = "0.2.0"
edition = "2021"
authors = ["DeepLink-MLSysResearch"]
license = "MIT"
description = "Rust bindings for NanoSequence C++ library"

[lib]
name = "nanosequence_rs"
crate-type = ["cdylib", "rlib"]

[dependencies]
libc = "0.2"

[build-dependencies]
cc = "1.0"

[features]
default = []
