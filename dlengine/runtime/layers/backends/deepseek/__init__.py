"""DeepGEMM/DeepEP optimized linear and expert implementations.

These are the FP8 vendor implementations (require SM90+). Selection policy lives
elsewhere; this package only owns the implementations.
"""
