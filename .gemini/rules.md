# Project Rules

1. **Documentation Storage**: All generated intermediate documents (designs, plans, reports) must be saved in the `docs/` directory of the project, ensuring a permanent record of the development process.
2. **Execution Environment**: Development is performed on a local machine, but testing runs on a separate deployment machine. Therefore, complex build or execution commands should be suggested for manual execution rather than auto-run.
