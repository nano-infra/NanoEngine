# Kernel package — hardware-agnostic kernels live here directly;
# Hopper-specific kernels (fp8, fused_moe_v3, etc.) are also accessible
# via nanodeploy.backends.hopper.kernels for clean namespace separation.
