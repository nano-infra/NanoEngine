[server]
host = "0.0.0.0"
port = 3000
model_name = "Proxima-7B"

# Engine Connection
engine_host = "127.0.0.1"
engine_port = 5000

[tokenizer]
path = "/home/majinming/models/qwen3-0.6b-local/tokenizer.json"

[engine]
mode = "Disaggregated"

[engine.prefill]
count = 1
config_path = "/models/configs/prefill.json"
tensor_parallel = 1

[engine.decode]
count = 2
config_path = "/models/configs/decode.json"
tensor_parallel = 2

[scheduler]
queue_size = 1000
timeout_ms = 30000
